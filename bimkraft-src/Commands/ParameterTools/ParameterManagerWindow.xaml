<Window x:Class="BIMKraft.Windows.ParameterManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BIM Kraft Project Parameters Manager"
        Width="1000" Height="750"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <Window.Resources>
        <!-- Styles -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="SubHeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="Gray"/>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="DodgerBlue"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="BIM Kraft Project Parameters Manager" Style="{StaticResource HeaderTextStyle}"/>
            <TextBlock Text="Select parameters from shared parameter file and configure their application"
                      Style="{StaticResource SubHeaderTextStyle}" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" MinWidth="300"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="1*" MinWidth="350"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Parameter Selection -->
            <GroupBox Grid.Column="0" Header="Available Parameters" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Search Box -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="Search Parameters:" Style="{StaticResource SectionHeaderStyle}"/>
                        <TextBox x:Name="SearchTextBox"
                                 TextChanged="SearchTextBox_TextChanged"
                                 Padding="5,3"
                                 Background="White"/>
                        <TextBlock Text="Type to filter parameters by name"
                                   FontSize="10" Foreground="Gray" Margin="0,3,0,0"/>
                    </StackPanel>

                    <!-- Parameters Tree -->
                    <TreeView x:Name="ParametersTree" Grid.Row="1"
                              BorderBrush="LightGray" BorderThickness="1"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"/>

                    <!-- Selection Summary -->
                    <TextBlock x:Name="SelectionSummary" Grid.Row="2"
                               Margin="0,10,0,0"
                               FontSize="11" Foreground="DarkBlue"
                               TextWrapping="Wrap"
                               Text="No parameters selected"/>
                </Grid>
            </GroupBox>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1"
                         HorizontalAlignment="Stretch"
                         Background="LightGray"
                         ShowsPreview="True"/>

            <!-- Right Panel: Configuration -->
            <GroupBox Grid.Column="2" Header="Parameter Configuration" Padding="10">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="ConfigStackPanel">
                        <TextBlock Text="Select parameters from the tree to configure them"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontStyle="Italic"
                                   Foreground="Gray"
                                   Margin="0,50,0,0"/>
                    </StackPanel>
                </ScrollViewer>
            </GroupBox>
        </Grid>

        <!-- Presets Section -->
        <GroupBox Grid.Row="2" Header="Presets" Margin="0,15,0,15" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="PresetsComboBox"
                         Grid.Column="0"
                         Margin="0,0,10,0"
                         VerticalContentAlignment="Center"
                         Padding="5"/>

                <Button Grid.Column="1"
                       Content="Load Preset"
                       Click="LoadPresetButton_Click"
                       Style="{StaticResource SecondaryButtonStyle}"/>

                <Button Grid.Column="2"
                       Content="Save Preset"
                       Click="SavePresetButton_Click"
                       Style="{StaticResource SecondaryButtonStyle}"/>

                <Button Grid.Column="3"
                       Content="Delete Preset"
                       Click="DeletePresetButton_Click"
                       Style="{StaticResource SecondaryButtonStyle}"/>
            </Grid>
        </GroupBox>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3"
                   Orientation="Horizontal"
                   HorizontalAlignment="Right">
            <CheckBox x:Name="UpdateExistingCheckBox"
                     Content="Update Existing Parameters"
                     VerticalAlignment="Center"
                     Margin="0,0,15,0"
                     ToolTip="If checked, existing parameters will be updated. If unchecked, existing parameters will be skipped."/>
            <CheckBox x:Name="MergeCategoriesCheckBox"
                     Content="Merge Categories"
                     VerticalAlignment="Center"
                     Margin="0,0,15,0"
                     ToolTip="If checked, new categories will be merged with existing ones. If unchecked, categories will be replaced."/>
            <Button Content="Add Parameters to Project"
                   Click="AddParametersButton_Click"
                   Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="Cancel"
                   Click="CancelButton_Click"
                   Style="{StaticResource SecondaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
